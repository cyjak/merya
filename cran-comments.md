## Submission

This is the first submission of merya to CRAN.

## Test environments

* local: (fill in after running `R CMD check --as-cran` locally)
* GitHub Actions: ubuntu-latest, windows-latest, macOS-latest (release R)
* win-builder (devel and release) -- run `devtools::check_win_devel()`
  and `devtools::check_win_release()` before submitting

## R CMD check results

0 errors | 0 warnings | 0 notes (fill in with the actual output before
submitting).

## Downstream dependencies

This is a new package; there are no downstream dependencies.
